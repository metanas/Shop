{% if categories %}
    {% set category = categories|first %}
        <a href="{{ category.href }}"
           class="menu-default {{ category.isActive }}"><span>{{ category.name }}</span></a>
{% endif %}