<select id="myBtn" name="option[{{ option.product_option_id }}]" id="input-option{{ option.product_option_id }}" class="form-control">
                        <option value="">Votre taille</option>
                        {% for option_value in option.product_option_value %}
                            <option value="{{ option_value.product_option_value_id }}">{{ option_value.name }}
                                {% if option_value.price %}
                                    ({{ option_value.price_prefix }}{{ option_value.price }})
                                {% endif %} </option>
                        {% endfor %}

                    </select>

                    <!-- The Modal -->
                    <div id="myModal" class="modal">

                        <!-- Modal content -->
                        <div class="modal-content col-md-6 col-md-offset-6 col-sm-12">
                            <span class="close">&times;</span>
                            {% for option_value in option.product_option_value %}
                                <button value="{{ option_value.product_option_value_id }}">{{ option_value.name }}
                                </button>
                            {% endfor %}
                        </div>

                    </div>